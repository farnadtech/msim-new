# رفع باگ‌های تحویل فیزیکی سیمکارت

## باگ‌های رفع شده

### 1. ✅ نمایش شماره تماس فروشنده به ادمین
**مشکل**: ادمین شماره تماس فروشنده را نمی‌دید

**راه‌حل**:
- فیلد `seller_phone` به جدول `activation_requests` اضافه شد
- تابع `createActivationRequest` به‌روزرسانی شد تا شماره فروشنده را از جدول `users` بگیرد و ذخیره کند
- پنل ادمین به‌روزرسانی شد تا شماره فروشنده را در بخش "📞 اطلاعات تماس فروشنده" نمایش دهد

**فایل‌های تغییر یافته**:
- `supabase/add-seller-phone-to-activation-requests.sql` (جدید)
- `services/api-supabase.ts` (تابع createActivationRequest)
- `pages/AdminActivationRequestsPanel.tsx` (مودال جزئیات)

---

### 2. ✅ نمایش پیام "در حال بررسی توسط ادمین" برای تحویل فیزیکی
**مشکل**: وقتی فروشنده کد را برای تحویل فیزیکی می‌فرستاد، به خریدار و فروشنده نشان داده نمی‌شد که کد در حال بررسی است

**راه‌حل**:
- تابع `sendActivationCodeForZeroLine` به‌روزرسانی شد:
  - برای تحویل فیزیکی: وضعیت `pending` می‌ماند (نه `activated`)
  - نوتیفیکیشن مناسب برای خریدار، فروشنده و ادمین ارسال می‌شود
  - پیام "⏳ کد در حال بررسی" به همه نشان داده می‌شود

**پیام‌های جدید**:
- **خریدار**: "کد فعال‌سازی توسط فروشنده ارسال شد و در حال بررسی توسط ادمین است"
- **فروشنده**: "کد فعال‌سازی ارسال شد و در حال بررسی توسط ادمین است"
- **ادمین**: "کد فعال‌سازی جدید برای بررسی (تحویل فیزیکی)"

**فایل‌های تغییر یافته**:
- `services/api-supabase.ts` (تابع sendActivationCodeForZeroLine)
- `pages/BuyerOrderTrackingPage.tsx` (نمایش پیام "در حال بررسی")
- `components/SellerInactiveOrdersPanel.tsx` (نمایش پیام راهنما)

---

### 3. ✅ نمایش کد فعال‌سازی به ادمین (نه "تحویل فیزیکی")
**مشکل**: در جدول ادمین، به جای کد فعال‌سازی، "📦 تحویل فیزیکی" نمایش داده می‌شد

**راه‌حل**:
- منطق نمایش در جدول تغییر کرد:
  - اگر کد وجود دارد → نمایش کد
  - اگر کد وجود ندارد و تحویل فیزیکی است → "📦 تحویل فیزیکی"
  - اگر کد وجود ندارد → "هنوز ارسال نشده"

- در مودال جزئیات، کد همیشه نمایش داده می‌شود (برای هر دو نوع تحویل)

**فایل‌های تغییر یافته**:
- `pages/AdminActivationRequestsPanel.tsx` (جدول و مودال)

---

### 4. ✅ وضعیت صحیح برای ادمین
**مشکل**: وضعیت به "فعال شده" تغییر می‌کرد در حالی که باید "در انتظار" می‌ماند

**راه‌حل**:
- برای تحویل فیزیکی:
  - `activation_requests.status` = `pending` (نه `activated`)
  - `purchase_orders.status` = `pending` (نه `code_sent`)
- برای تحویل با کد:
  - `activation_requests.status` = `activated`
  - `purchase_orders.status` = `code_sent`

**فایل‌های تغییر یافته**:
- `services/api-supabase.ts` (تابع sendActivationCodeForZeroLine)

---

## فلوی کامل سیستم (به‌روز شده)

### تحویل با کد فعال‌سازی:
1. خریدار سیمکارت صفر را خریداری می‌کند
2. خریدار "دریافت کد فعال‌سازی" را انتخاب می‌کند
3. فروشنده کد را ارسال می‌کند
4. **وضعیت به `code_sent` تغییر می‌کند**
5. خریدار کد را می‌بیند و تایید می‌کند
6. پول به فروشنده واریز می‌شود

### تحویل فیزیکی:
1. خریدار سیمکارت صفر را خریداری می‌کند
2. خریدار "دریافت نسخه فیزیکی" را انتخاب می‌کند و آدرس را وارد می‌کند
3. فروشنده کد را ارسال می‌کند
4. **وضعیت `pending` می‌ماند**
5. **خریدار پیام "⏳ کد در حال بررسی توسط ادمین" را می‌بیند**
6. **فروشنده پیام "⏳ کد در حال بررسی توسط ادمین" را می‌بیند**
7. **ادمین کد را می‌بیند** (نه "تحویل فیزیکی")
8. **ادمین شماره تماس فروشنده و خریدار را می‌بیند**
9. ادمین کد را تایید یا رد می‌کند
10. پس از تایید، سیمکارت به آدرس خریدار ارسال می‌شود

---

## فایل‌های جدید

1. ✅ `supabase/add-seller-phone-to-activation-requests.sql` - اضافه کردن فیلد شماره فروشنده

---

## فایل‌های به‌روز شده

1. ✅ `services/api-supabase.ts`:
   - تابع `createActivationRequest`: ذخیره شماره فروشنده
   - تابع `sendActivationCodeForZeroLine`: مدیریت وضعیت و نوتیفیکیشن‌ها

2. ✅ `pages/AdminActivationRequestsPanel.tsx`:
   - جدول: نمایش کد به جای "تحویل فیزیکی"
   - مودال: نمایش شماره فروشنده و کد برای همه

3. ✅ `pages/BuyerOrderTrackingPage.tsx`:
   - نمایش پیام "در حال بررسی" برای تحویل فیزیکی

4. ✅ `components/SellerInactiveOrdersPanel.tsx`:
   - نمایش پیام راهنما برای فروشنده

---

## تست سیستم

### چک‌لیست تست:
- [ ] ادمین شماره تماس فروشنده را می‌بیند
- [ ] وقتی فروشنده کد را برای تحویل فیزیکی می‌فرستد:
  - [ ] خریدار پیام "در حال بررسی" را می‌بیند
  - [ ] فروشنده پیام "در حال بررسی" را می‌بیند
  - [ ] ادمین نوتیفیکیشن دریافت می‌کند
- [ ] ادمین کد فعال‌سازی را می‌بیند (نه "تحویل فیزیکی")
- [ ] وضعیت در پنل ادمین "در انتظار" است (نه "فعال شده")
- [ ] برای تحویل با کد، همه چیز مثل قبل کار می‌کند

---

## نکات مهم

1. **کد همیشه به ادمین نشان داده می‌شود** - برای هر دو نوع تحویل
2. **خریدار کد را نمی‌بیند** - فقط پیام "در حال بررسی" یا "تحویل فیزیکی"
3. **وضعیت pending می‌ماند** - تا ادمین تایید کند
4. **شماره تماس فروشنده** - به ادمین نمایش داده می‌شود
5. **نوتیفیکیشن‌های مناسب** - به همه طرف‌ها ارسال می‌شود
