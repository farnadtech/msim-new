# 🚀 راهنمای سریع: سیستم احراز هویت (KYC)

## شروع سریع در 5 دقیقه

### مرحله 1: اجرای SQL ها (2 دقیقه)

#### 1.1 ایجاد جدول KYC
در Supabase SQL Editor اجرا کنید:
```sql
-- کپی و اجرا: supabase/create-kyc-verification-table.sql
```

#### 1.2 افزودن تنظیمات
در Supabase SQL Editor اجرا کنید:
```sql
-- کپی و اجرا: supabase/add-activation-settings.sql
```

#### 1.3 ایجاد Storage Bucket
⚠️ **توجه**: این مرحله از Dashboard انجام می‌شود، نه SQL Editor!

**مراحل**:
1. به Supabase Dashboard بروید
2. Storage > New bucket
3. تنظیمات:
   - Name: `kyc-documents`
   - Public: ✓
   - File size limit: 10 MB
   - MIME types: image/jpeg, image/jpg, image/png, image/webp
4. Create bucket

**سپس** RLS Policies را از SQL Editor اجرا کنید:
```sql
-- کپی و اجرا: supabase/create-kyc-storage-bucket.sql
```

📖 **راهنمای کامل**: `STORAGE_BUCKET_SETUP_GUIDE.md`

### مرحله 2: تنظیمات اولیه (1 دقیقه)

1. وارد پنل ادمین شوید
2. به تنظیمات بروید
3. دسته "احراز هویت" را باز کنید
4. تنظیمات را فعال کنید:
   - ✅ `kyc_required` = true
   - ✅ `kyc_required_for_sellers` = true
   - ✅ `kyc_required_for_buyers` = true
5. دسته "عمومی" را باز کنید
6. `buyer_can_receive_activation_code` = false (پیش‌فرض)

### مرحله 3: تست (2 دقیقه)

1. **تست کاربر**:
   - حساب جدید بسازید
   - سعی کنید خرید کنید
   - فرم KYC را پر کنید
   - تصاویر آپلود کنید

2. **تست ادمین**:
   - به `/admin/kyc-management` بروید
   - درخواست را ببینید
   - تایید کنید

3. **تست دسترسی**:
   - با حساب کاربری وارد شوید
   - بررسی کنید که دسترسی دارید

## ✅ آماده است!

سیستم شما آماده استفاده است.

## 📚 مستندات کامل

- `KYC_AND_ACTIVATION_SYSTEM_GUIDE.md` - راهنمای کامل
- `IMPLEMENTATION_SUMMARY_KYC.md` - خلاصه پیاده‌سازی
- `KYC_DEPLOYMENT_CHECKLIST.md` - چک لیست استقرار

## 🎯 نکات کلیدی

### برای ادمین
- درخواست‌های KYC را از `/admin/kyc-management` مدیریت کنید
- همیشه دلیل رد را بنویسید
- تصاویر را با دقت بررسی کنید

### برای کاربران
- تصاویر باید واضح باشند
- اطلاعات باید صحیح باشند
- در صورت رد، می‌توانید مجددا ارسال کنید

### تنظیمات مهم
- `buyer_can_receive_activation_code`: کنترل دریافت کد فعال‌سازی
- `kyc_required`: الزامی بودن احراز هویت
- `kyc_required_for_sellers`: برای فروشندگان
- `kyc_required_for_buyers`: برای خریداران

## 🐛 مشکلات رایج

### تصاویر آپلود نمی‌شوند
```sql
-- بررسی Storage Bucket
SELECT * FROM storage.buckets WHERE id = 'kyc-documents';
```

### کاربر نمی‌تواند ببیند
```sql
-- بررسی RLS
SELECT * FROM kyc_verifications WHERE user_id = 'USER_ID';
```

### تنظیمات کار نمی‌کنند
- Cache را پاک کنید
- صفحه را Refresh کنید

## 📞 نیاز به کمک؟

1. مستندات را بخوانید
2. لاگ‌های Console را بررسی کنید
3. Supabase Dashboard را بررسی کنید

---

**موفق باشید! 🎉**
